## 1. State and color presentation

- [x] 1.1 Extend the Tag color type and strict state parser with canonical lowercase six-digit HEX values.
- [x] 1.2 Add shared parsing, normalization, tonal-fill, contrast, and CSS-variable helpers for arbitrary colors with unit coverage.
- [x] 1.3 Apply named and custom color surfaces consistently to every Tag-bearing component.

## 2. Tag catalog color control

- [x] 2.1 Replace the fixed Select with an accessible Popover dropdown containing the full palette above named presets and No color.
- [x] 2.2 Localize custom palette labels and accessibility copy in all six catalogs.
- [x] 2.3 Add component/browser coverage for arbitrary choice, preset choice, reset, persistence, and light/dark rendering.

## 3. Documentation and gallery

- [x] 3.1 Update usage, architecture, privacy, performance, README, and screenshot guidance for named and custom Tag colors.
- [x] 3.2 Regenerate all 46 PNGs, visually inspect affected Tag surfaces and the open color dropdown, regenerate again, and compare SHA-256 hashes.

## 4. Validation and lifecycle

- [x] 4.1 Run format, lint, typecheck, unit, development, server/Pages builds, browser suites, Pages/public checks, strict OpenSpec validation, and diff checks.
- [x] 4.2 Synchronize capability specs, archive the change, validate the empty active-change list, commit, merge to main, push, delete the branch, and verify a clean synchronized repository.
