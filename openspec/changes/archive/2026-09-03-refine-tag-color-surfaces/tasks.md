## 1. Shared Tag surfaces

- [x] 1.1 Replace the dot color helper with shared accessible light/dark surface classes and unit coverage for every palette value.
- [x] 1.2 Apply filled Tag surfaces without leading dots to Employee chips, assignment controls, the Tag catalog, palette choices, and Calendar controls.

## 2. Validation and documentation

- [x] 2.1 Extend browser coverage for colored and neutral Tag surfaces, interaction states, both themes, and absence of marker dots.
- [x] 2.2 Update usage and screenshot documentation, then regenerate and visually inspect all 46 PNGs twice with identical hashes.
- [x] 2.3 Run format, lint, typecheck, unit, dev check, both builds, both browser suites, Pages/public checks, strict OpenSpec validation, and diff check.

## 3. Delivery

- [x] 3.1 Synchronize canonical specs, archive the change, commit, fast-forward merge to main, push, remove the change branch, and verify a clean synchronized repository.
