## 1. Exact Tag color entry

- [x] 1.1 Extend the strict Tag color type and state parser for canonical eight-digit HEX values while retaining named and six-digit colors.
- [x] 1.2 Add dependency-free parsing and formatting for HTML Keyword, HEX, RGB, and RGBA input, including alpha-aware tonal surface derivation.
- [x] 1.3 Extend the shared Tag color dropdown with type selection, type-specific input, validation, and synchronized palette/preset behavior.
- [x] 1.4 Add focused unit coverage for canonicalization, bounds, invalid input, keyword resolution, alpha rendering, and strict state rejection.

## 2. Tag catalog editing

- [x] 2.1 Replace the inline catalog editor with a dedicated Tag edit Dialog using an isolated draft and explicit Save/Cancel behavior.
- [x] 2.2 Verify focus, cancellation, invalid draft behavior, persistence, reset, and named/custom colors in server and Pages browser flows.

## 3. Token-aware Format guidance

- [x] 3.1 Add a compact accessible help icon and localized tooltip after the label in the shared Template Format input.
- [x] 3.2 Add localized `@` discovery placeholder copy without changing caret, keyboard, token insertion, or formatter behavior.
- [x] 3.3 Cover Data Download, Editor Template export, and Employee Template definitions in component and browser tests.

## 4. Localization, documentation, and gallery

- [x] 4.1 Add and audit matching copy and placeholders in all six locale catalogs.
- [x] 4.2 Update README, architecture, usage, privacy, performance, and screenshot documentation.
- [x] 4.3 Refresh the affected Tag editor and Template guidance frames while preserving the declared 46-PNG gallery.

## 5. Verification and delivery

- [x] 5.1 Run format, lint, typecheck, unit, development, both production builds, and both browser suites without diagnostics.
- [x] 5.2 Generate the 46-PNG gallery twice, visually inspect affected and representative frames, and compare deterministic hashes.
- [x] 5.3 Run Pages/public checks, strict OpenSpec validation, and diff checks; inspect the staged set for publication hazards.
- [x] 5.4 Synchronize canonical specs, archive the change with no active changes, commit, merge to main, push, delete the change branch, and verify clean synchronized delivery.
