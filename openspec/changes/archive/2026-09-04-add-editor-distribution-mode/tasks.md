## 1. State contract and persistence

- [x] 1.1 Add required per-View distribution Unit IDs to the strict state, blank state, fixtures, validation, and bounded UI synchronization
- [x] 1.2 Remap distribution Unit IDs when cloning a View, exclude them from cross-View Unit Paste, and prune them through every Unit deletion path
- [x] 1.3 Back up and convert the configured local SQLite state offline, then validate the result with the production parser

## 2. Distribution derivation and visualization

- [x] 2.1 Implement cached direct manual/Live Employee membership indexing and constant-time row status derivation
- [x] 2.2 Add the accessible context-menu checkbox and persisted per-Unit toggle without structural history or geometry changes
- [x] 2.3 Add semantic light/dark green and amber row states that preserve their fill during hover and selection
- [x] 2.4 Implement deterministic expanded-row and collapsed-Unit anchors plus the pointer-inert single-selection connection overlay
- [x] 2.5 Keep distribution visualization out of Editor PNG, JSON, Template, and Employee exports

## 3. Localization, tests, and documentation

- [x] 3.1 Add complete EN, ZH, RU, ES, FR, and AR messages and accessibility text
- [x] 3.2 Add unit coverage for state validation, View lifecycle, membership, status gating, anchors, routing, cleanup, and output isolation
- [x] 3.3 Add server/Pages browser coverage for mouse and keyboard toggles, multiple Units, themes, locales, connections, reload, tab sync, View switching, Import/Export, and diagnostics
- [x] 3.4 Update architecture, usage, privacy, performance, import-format, README, screenshot guidance, and capability specifications
- [x] 3.5 Add the two distribution scenarios, regenerate all 56 PNGs twice, visually inspect them, and compare deterministic hashes

## 4. Validation and delivery

- [x] 4.1 Run format, lint, typecheck, unit, dev check, both production builds, browser suites, Pages/public checks, strict OpenSpec validation, and diff check
- [x] 4.2 Synchronize canonical specs, archive the completed change, commit, fast-forward into main, push, remove the merged branch, and verify a clean synchronized repository
