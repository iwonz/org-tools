## 1. Distribution controls and placement map

- [x] 1.1 Implement tri-state bulk distribution-mode updates for selected Units with one UI mutation
- [x] 1.2 Add the multi-Unit Employee row action and shared occurrence-navigation coordinator
- [x] 1.3 Implement the transient read-only placement-map modal with deterministic layout and viewport controls
- [x] 1.4 Add focused unit tests for membership derivation, bulk state, map layout, and navigation

## 2. Tag discovery and catalog density

- [x] 2.1 Add shared locale-aware Tag ordering and searchable virtualized Tag filter options
- [x] 2.2 Make Select all and Deselect all operate on visible search results without changing hidden selections or Without tags
- [x] 2.3 Place Tag catalog Employee and dated counts inline beside each Tag with stable narrow geometry
- [x] 2.4 Add six-locale copy and unit coverage for Tag search, sorting, bulk actions, and catalog layout

## 3. Browser coverage, documentation, and gallery

- [x] 3.1 Extend server and Pages browser suites for bulk distribution, placement navigation, Tag search, catalog counts, RTL, responsive layout, diagnostics, and large-model bounds
- [x] 3.2 Update architecture, usage, performance, screenshot documentation, README count, and capability deltas
- [x] 3.3 Add bulk-distribution and placement-map scenarios, regenerate all 58 PNGs twice, visually inspect them, and compare SHA-256 manifests

## 4. Validation and delivery

- [x] 4.1 Run format, lint, typecheck, unit, dev check, both builds and browser suites, Pages/public checks, strict OpenSpec validation, and diff check
- [x] 4.2 Synchronize canonical specs, archive the change, validate no active changes, commit, merge into main, push, remove the merged branch, and verify a clean synchronized worktree
