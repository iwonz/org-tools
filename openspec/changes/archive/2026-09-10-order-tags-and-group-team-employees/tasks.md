## 1. Ordered catalog and grouping model

- [x] 1.1 Add the shared catalog priority index and preserve Tag order in derived Employees, controls, Calendar, and all exports; cover ordering and search with unit tests.
- [x] 1.2 Add strict Unit groupByTag, default creation, shared Employee grouping, history, cloning, fixtures, and tests for validation and geometry.

## 2. User controls

- [x] 2.1 Add atomic catalog drag/drop and keyboard moves, conditional dated counts, modal color-picker scrolling, and six-locale copy.
- [x] 2.2 Add accessible Unit settings and immediate undoable grouping switch; verify hover, touch, focus, and canvas event isolation.

## 3. Verification and documentation

- [x] 3.1 Add both-runtime browser coverage and gallery settings scenario; update architecture, usage, performance, transfer, and screenshot documentation.
- [x] 3.2 Verify temporary conversion against synthetic data including failure rollback, then back up and convert the configured local database outside runtime.
- [x] 3.3 Run format, lint, typecheck, unit tests, development check, production build, browser tests, screenshot generation, Pages build/check, public check, strict spec validation, and diff check. Inspect every PNG and compare a second generation's hashes; preserve the large-data target.

## 4. Closed delivery

- [x] 4.1 Synchronize capability deltas, archive the change, validate strictly, and verify no active changes.
- [x] 4.2 Commit, fetch and integrate origin/main, merge and push main, delete the merged working branch, and verify clean synchronized HEAD/main/origin/main.
