## 1. Derived Editor behavior

- [x] 1.1 Derive ordinary-only direct membership and use source-aware placement actions and live modal eligibility.
- [x] 1.2 Fix shared unique subtree totals and verify all hierarchy count consumers.
- [x] 1.3 Split layout direction into explicit accessible buttons using existing undoable arrangement.
- [x] 1.4 Cover membership, counts, direction history, Live Units, and scale with focused tests.

## 2. Tag pointer sorting

- [x] 2.1 Implement captured pointer sorting with full-row overlay, stable insertion slots, animated displacement, and edge scrolling.
- [x] 2.2 Commit one previewed move, support filtered insertion, and cancel stale/interrupted gestures with focus and keyboard accessibility.
- [x] 2.3 Exercise pointer, keyboard, cancellation, touch, persistence, and peer synchronization in both runtimes.

## 3. Documentation and verification

- [x] 3.1 Update documentation, capability deltas, and all affected six-locale messages.
- [x] 3.2 Run format, lint, typecheck, unit tests, development check, production build, and both browser suites.
- [x] 3.3 Generate and inspect every gallery PNG, regenerate, and confirm identical hashes.
- [x] 3.4 Run Pages build/check, public check, strict OpenSpec validation, and diff checks.

## 4. Delivery

- [x] 4.1 Synchronize canonical specifications and validate the completed artifacts for archival.
- [x] 4.2 Record validation evidence and prepare the reviewed diff for the authorized main integration.

Delivery continues after these artifact tasks: archive the completed change, commit it, integrate
origin/main, merge and push main, delete the merged branch, and verify clean synchronized refs with
no active OpenSpec changes.
