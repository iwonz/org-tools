## 1. Diagnosis and delivery contract

- [x] 1.1 Confirm the configured singleton has the exact prior View shape, capture non-sensitive preservation fingerprints, and verify `canvasElements` is the only diagnosed schema gap.
- [x] 1.2 Update AGENTS.md plus architecture, usage, and privacy documentation so strict State-shape delivery explicitly prepares the configured owned database offline without runtime compatibility.
- [x] 1.3 Retain or extend automated coverage proving the previous View shape remains rejected by runtime parsing and no automatic migration is introduced.

## 2. Guarded local repair

- [x] 2.1 Stop checkout-owned runtimes, create a timestamped ignored backup of the complete configured SQLite family, and validate the transformed detached candidate with the production parser.
- [x] 2.2 Add empty `canvasElements` arrays in one immediate transaction, increment revision exactly once, and verify all preservation fingerprints plus production reopening and configured-server startup.

## 3. Validation and delivery

- [x] 3.1 Run the complete mandatory repository checks, both production browser suites, and two deterministic 59-frame gallery generations with visual and hash verification.
- [x] 3.2 Synchronize canonical specs, archive the change, merge and push `main`, delete the merged branch, and verify a clean synchronized repository with no active changes.
