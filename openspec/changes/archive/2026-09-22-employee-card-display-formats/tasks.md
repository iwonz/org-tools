## 1. State and format model

- [x] 1.1 Add exact Employee display-format types, defaults, strict parsing, blank-state creation, store load/save, persistence observation, and State round-trip coverage.
- [x] 1.2 Implement shared contextual Employee display rendering, ordered aggregate Unit values, line normalization, and custom-field dependency rewrite/delete checks with unit tests.

## 2. Product surfaces

- [x] 2.1 Add Model and Display tabs, four draft format editors with live previews and atomic Save, flat Required rows, and complete six-locale copy.
- [x] 2.2 Apply Employees and Units formats to shared card contexts while retaining actions, search highlighting, accessibility, and safe explicit navigation.
- [x] 2.3 Apply Editor and Editor-export formats with shared multiline row geometry, transient PNG overrides, custom tokens, and unchanged open-position behavior.

## 3. Coverage and documentation

- [x] 3.1 Add browser coverage for editing, cancel/save persistence, context selection, fallback cards, Editor rows, PNG defaults/overrides, State transfer, and live-tab/runtime behavior.
- [x] 3.2 Update architecture, usage, performance, privacy, screenshot documentation, and the fixed 59-frame screenshot workflow.
- [x] 3.3 Stop the owned runtime, back up and convert the configured prior SQLite snapshot offline, validate detached and committed State with the production parser, and prove normal startup.

## 4. Validation and delivery

- [x] 4.1 Run formatting, lint, typecheck, unit tests, development probe, production/browser builds and tests, public checks, strict OpenSpec validation, and diff checks.
- [x] 4.2 Generate the screenshot gallery twice, visually inspect every PNG, and confirm deterministic hashes and maintained scale behavior.
- [x] 4.3 Synchronize and archive the OpenSpec change, integrate current origin/main, merge and push main, delete the merged branch, and verify clean synchronized completion.
