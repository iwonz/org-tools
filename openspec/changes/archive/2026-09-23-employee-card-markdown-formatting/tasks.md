## 1. Rich display model

- [x] 1.1 Add direct Markdown parser dependencies and implement a cached, inert rich-line compiler that preserves semantic Tag and position nodes.
- [x] 1.2 Add unit coverage for Markdown marks, safe and unsafe links, inert field values, conditions, custom fields, native Tags, and ordered positions.

## 2. Display editor and list cards

- [x] 2.1 Add opt-in accessible selection Markdown tools with toggle operations and create, edit, validate, and remove link workflows.
- [x] 2.2 Add Model and Display icons, flatten Display sections, localize all new copy in six catalogs, and render equal-base-style rich list cards with native chips and safe actions.

## 3. Editor and PNG parity

- [x] 3.1 Render rich non-interactive Editor rows and derive row, hit-test, anchor, and Unit geometry from measured text and semantic-chip wrapping.
- [x] 3.2 Paint matching Markdown runs, links, Tag chips, and position badges in scoped and full-View PNG output with shared geometry tests.

## 4. Coverage and documentation

- [x] 4.1 Add browser coverage for toolbar selection, dismissal, link lifecycle, token-menu arbitration, flat sections, icons, four contexts, navigation, search, Editor geometry, and PNG overrides.
- [x] 4.2 Update architecture, usage, performance, privacy, screenshots, and deterministic 59-frame gallery coverage.

## 5. Validation and delivery

- [x] 5.1 Run formatting, lint, typecheck, unit tests, development probe, production/browser builds and tests, Pages and public checks, strict OpenSpec validation, and diff checks.
- [x] 5.2 Generate the screenshot gallery twice, visually inspect every PNG, and confirm identical hashes and maintained scale behavior.
- [x] 5.3 Synchronize and archive the OpenSpec change, integrate current origin/main, merge and push main, delete the merged branch, and verify clean synchronized completion.
