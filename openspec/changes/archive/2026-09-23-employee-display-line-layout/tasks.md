## 1. State and token contract

- [x] 1.1 Add four bounded Employee display line gaps to types, defaults, strict State parsing, store capture/load/save, transfer, synchronization, and tests.
- [x] 1.2 Split Employee-owned and Unit-context token constants, scope Template suggestions correctly, and render the two localized Model groups.
- [x] 1.3 Make Employee display `isBoss` condition-only, localize creation-time default ternaries, and remove every image boss-label setting, control, validation, and obsolete message.

## 2. Rich display layout

- [x] 2.1 Preserve intentional internal blank rows and implement bounded word/character wrapping for rich text without interpreting Employee values as Markdown.
- [x] 2.2 Add one 0-24 px slider and numeric readout per Display section, with live preview plus atomic draft save/cancel behavior.
- [x] 2.3 Apply the matching format and line gap to Employees, Units, fallback cards, Unit drag previews, Editor DOM, and both Editor image flows.

## 3. Editor and PNG parity

- [x] 3.1 Introduce shared width-aware rich-row packing and update Editor row heights, offsets, virtualization, hit testing, anchors, and Unit bounds.
- [x] 3.2 Render the shared wrapped rows and configured gaps in PNG while preserving native Tag and assignment-pill geometry, open positions, and Unit Tag clouds.
- [x] 3.3 Add focused unit and browser coverage for blank rows, wrapping, gap extremes, Markdown, semantic nodes, boss ternaries, dynamic geometry, and DOM/PNG parity.

## 4. Product surfaces and migration

- [x] 4.1 Update all six locale catalogs, usage, architecture, performance, privacy, screenshots documentation, and screenshot demo coverage.
- [x] 4.2 Stop the owned runtime, back up and convert the configured SQLite family offline, validate candidate and committed State with the production parser, compare preserved data, and prove reopen.

## 5. Validation and delivery

- [x] 5.1 Run formatting, lint, typecheck, unit, development, build, browser, Pages, public-safety, strict OpenSpec, and diff checks, including the 20,000/4,000 performance target.
- [x] 5.2 Generate the 59-PNG gallery twice, compare deterministic hashes, and visually inspect every image.
- [x] 5.3 Sync and archive OpenSpec, validate no active changes, integrate current origin/main, merge and push main, remove the completed branch, and verify a clean synchronized repository.
