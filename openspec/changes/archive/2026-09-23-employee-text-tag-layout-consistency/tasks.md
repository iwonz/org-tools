## 1. Shared measurement and surface contract

- [x] 1.1 Replace density-specific Tag metrics with the one 11/16/8/2/6/6 contract and update the shared Tag surface API.
- [x] 1.2 Add a bounded actual-font Canvas measurement engine with semantic typography, deterministic test fallback, font invalidation, and unit coverage.
- [x] 1.3 Extend Employee rich layout and inline-surface layout to use actual measurements, atomic suffixes, and per-boundary semantic or format gaps.
- [x] 1.4 Cover exact content widths, suffix insets, multilingual wrapping, RTL, and mixed boundary gaps in unit tests.

## 2. Application DOM surfaces

- [x] 2.1 Render Employee list cards and previews from mounted UI-font measurements without fragment-level glyph clipping.
- [x] 2.2 Apply the universal Tag contract and 6 pixel collection gaps to forms, pickers, filters, catalog, Calendar, color previews, and drag surfaces.
- [x] 2.3 Render Display Reset actions at normal weight and add browser coverage for complete email and username suffixes plus computed Tag metrics.

## 3. Editor and PNG parity

- [x] 3.1 Pass shared real-font measurements through Editor Employee rows, open positions, Unit Tag footers, row stacks, virtualization, bounds, hit testing, and anchors.
- [x] 3.2 Pass the selected image font measurement through both PNG paths and draw the universal Tag, dated suffix, count suffix, and assignment geometry.
- [x] 3.3 Cover narrow cards, long multilingual Tags, fixed semantic gaps, selected fonts, collapsed Units, footer counts, and exact DOM/PNG layout parity.

## 4. Documentation and delivery

- [x] 4.1 Update architecture, usage, performance, privacy, screenshots, canonical capability deltas, and catalog-completeness validation without new copy.
- [x] 4.2 Run format, lint, typecheck, unit tests, development probe, builds, both browser runtimes, Pages validation, public-safety scan, strict OpenSpec validation, and diff checks.
- [x] 4.3 Generate the maintained 59-image gallery twice, compare deterministic hashes, and visually inspect every PNG.
- [x] 4.4 Record that State is unchanged and the current owned SQLite needs no conversion; sync and archive OpenSpec, integrate current origin/main, push main, delete the change branch, and verify a clean synchronized repository.
