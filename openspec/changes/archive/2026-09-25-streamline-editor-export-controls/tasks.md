## 1. Template export and exact State

- [x] 1.1 Remove Template row-mode types, controls, persistence, parser keys, fixtures, and branches; make both Template surfaces evaluate every retained Unit assignment while JSON remains grouped by Employee.
- [x] 1.2 Add the transient Keep only unique values checkbox and one shared exact logical-line processor for preview, counts, Copy, and Save, including async batch boundaries and empty-line ordering.
- [x] 1.3 Add unit and browser coverage for all-assignment output, fallback Employees, exact unique lines, line endings, whitespace, processed counts, both Template surfaces, and strict rejection of the obsolete State key.

## 2. Editor image export and toolbar

- [x] 2.1 Replace the Arrow toolbar glyph with the outlined up-right icon and update its focused browser and screenshot assertions.
- [x] 2.2 Remove density, title, title geometry, and output-font settings from both image dialogs and the renderer; request 3x output under the existing preview and final Canvas limits while preserving canvas-element typography.
- [x] 2.3 Replace both solid background inputs with the shared local color dropdown and resolve semantic, custom, used, and alpha colors for Canvas painting.
- [x] 2.4 Add unit and browser coverage for fixed density, clamps, removed controls, system structure typography, both color dropdowns, and complete local PNG generation.

## 3. Product text, documentation, and gallery

- [x] 3.1 Add Keep only unique values in all six locale catalogs and remove every obsolete row-mode message key while retaining shared image-setting messages still used elsewhere.
- [x] 3.2 Update architecture, usage, performance, privacy, screenshots, synthetic State fixtures, and OpenSpec task status for the simplified current behavior.
- [x] 3.3 Update the existing 59-frame gallery captures without changing the manifest or frame count.

## 4. Exact SQLite conversion

- [x] 4.1 Stop the owned runtime, retain a timestamped ignored backup of the complete configured SQLite family, and validate the immediately previous row and detached candidate.
- [x] 4.2 Remove only `ui.download.rowMode`, increment revision once in one transaction, compare preservation fingerprints, validate the committed row with the production parser, prove normal startup, and remove the temporary converter.

## 5. Validation and delivery

- [x] 5.1 Run formatting, lint, typecheck, unit tests, development probe, production build, and both-runtime browser tests.
- [x] 5.2 Generate the 59 PNG gallery twice, compare deterministic hashes, visually inspect every image, and run Pages build/check, public-safety scan, strict OpenSpec validation, and `git diff --check`.
- [x] 5.3 Synchronize capability deltas, archive the change, commit meaningfully, integrate fresh `origin/main`, merge and push `main`, delete the completed branch, and verify a clean synchronized repository with no active OpenSpec changes.
